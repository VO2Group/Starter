{
  "name": "starter",
  "version": "1.0.0",
  "description": "Write smart hybrid apps",
  "scripts": {
    "start": "browser-sync start -s app -f '**/*'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "browser-sync": "^2.13.0"
  }
}
