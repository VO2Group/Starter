{
  "name": "starter",
  "version": "1.0.0",
  "description": "Write smart hybrid apps",
  "scripts": {
    "start": "browser-sync start -s app -f '**/*'",
    "build" : "npm run build:ios",
    "build:ios": "cp -R app/ platforms/ios/web"
  },
  "devDependencies": {
    "browser-sync": "^2.13.0"
  }
}
